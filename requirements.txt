# Merlin — Python dependencies (v0.1)
# Python 3.11+ required.

typer>=0.12.0
httpx>=0.27.0
pydantic>=2.5.0
rich>=13.7.0
PyYAML>=6.0
jinja2>=3.1.0

# Optional generators — install only if you plan to use them:
# ollama>=0.3.0
# anthropic>=0.30.0
